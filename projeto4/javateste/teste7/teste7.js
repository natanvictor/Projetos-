function verificar () {
    window.alert('testando')
    var data = new Date()
    var ano = data.getFullYear
    var formano = document.getElementById('text')
    var res = document.querySelector('div#res')
    if (farmano.value.length ==0 || formano.value > ano) {
        
    }
}